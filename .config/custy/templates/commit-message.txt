fix(repository): resolve provider CLI paths on Windows

Use the executable path discovered during preflight when running repository
metadata synchronization commands.

Add regression coverage for Windows command resolution and preserve readable
provider command previews.
