feat(portal): checkpoint project previews and maintainer documentation

Record the second untagged v1.0.0 development checkpoint by adding a polished
visual introduction for every documented project and documenting how the
Devalltect portal itself is maintained.

### 🖼️ Project Visual Previews

- Reuse the shared screenshot component for Custy, Path Header Scanner,
  Doc Gen, and Reflow landing pages in both supported locales.
- Add overview and compact card presentation variants with consistent 16:9
  sizing, responsive containment, theme-aware surfaces, and accessible fallback
  behavior.
- Reserve stable asset paths under `static/img/project-previews/` so final PNG
  screenshots can be added without rewriting pages or project metadata.
- Keep missing preview assets non-breaking during preparation while requiring
  the real four images before the stable release is published.
- Detect image failures that occur before client hydration so cached missing
  assets still render the styled accessible fallback instead of broken-image UI.

### 🎨 Homepage and Project Metadata

- Display project previews directly in the existing homepage cards instead of
  creating a duplicate showcase section.
- Add typed preview metadata with asset paths, alternative text, and fallback
  descriptions to the centralized project catalog.
- Replace identical technology badges with concise project-specific runtime and
  tooling summaries.
- Preserve responsive card behavior, readable terminal images, lazy loading,
  and accessible navigation.
- Resolve the homepage hero logo through the configured Docusaurus base path so
  it renders correctly in GitHub Pages and other subpath deployments.

### 📘 Devalltect Docs Guide

- Add a dedicated Devalltect Docs documentation hub in English and Indonesian.
- Explain supported projects, portal organization, localization, source review,
  metadata ownership, screenshot capture, validation, freshness, and release
  preparation.
- Add the portal guide to the sidebar and homepage maintainer navigation.
- Correct outdated root-page language coverage and add the missing Indonesian
  introduction and project-overview pages.

### ✅ Release Preparation

- Document consistent PNG names and recommend crisp, credential-safe 1600 × 900
  terminal captures.
- Keep command names, paths, and configuration identifiers literal across
  translations.
- Validate documentation freshness, TypeScript, pre-commit, multilingual builds,
  routes, links, and responsive rendering before v1.0.0 publication.

Tag: none
Previous checkpoint: v1.0.0 development checkpoint 1
Release phase: v1.0.0 development checkpoint 2
Target release: v1.0.0

🔖 **Tags**:
- Type: `#feature`, `#documentation`, `#components`, `#i18n`
- Platform: `#docusaurus`, `#react`, `#typescript`, `#yarn`
- Projects: `#custy`, `#path-header-scanner`, `#doc-gen`, `#reflow`
- Experience: `#screenshots`, `#homepage`, `#accessibility`, `#responsive-ui`
- Quality: `#typecheck`, `#build`, `#freshness`, `#pre-commit`
- Stability: `#checkpoint`, `#untagged`, `#pre-release`
