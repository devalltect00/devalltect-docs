---
title: "Changelog System"
description: "Trace Custy's active changelog flow from Git ranges and message providers through semantic processing, release planning, Jinja rendering, and file output."
sidebar_position: 9
sidebar_key: custy-architecture-changelog-system
tags: [custy, changelog, architecture]
keywords: [Custy ChangelogGenerator, changelog pipeline, JinjaRenderer]
---

import {
    ArchitectureCard,
    CommandHero,
    CommandMetadata,
    DependencyCard,
    Grid,
    Icon,
    InfoBox,
    RelatedCommands,
    Section,
    WorkflowDiagram,
} from "@site/src/components/docs";

<CommandHero
    command="app/core/changelog/"
    title="Changelog System"
    description="Transform repository history and optional pending content into a structured, rendered CHANGELOG.md document."
    category="Architecture"
    status="Active Generation Flow"
    syntax="Git ranges -> providers -> processing -> releases -> Jinja -> Markdown"
    tags={["providers", "domain models", "processing pipeline", "rendering"]}
    links={[
        {label: "Generate Command", href: "../commands/changelog/generate/overview"},
        {label: "Configuration", href: "../configuration/changelog/overview"},
        {label: "Git Operations", href: "./git-operations"},
    ]}
/>

<CommandMetadata
    items={[
        {label: "Coordinator", value: "ChangelogGenerator", icon: "release"},
        {label: "History Boundary", value: "GitService", icon: "git"},
        {label: "Domain Output", value: "Changelog", icon: "architecture"},
        {label: "Renderer", value: "JinjaRenderer", icon: "template"},
    ]}
/>

## Overview

The active generator coordinates several focused components. It plans Git
ranges, creates message providers, converts raw messages into semantic commit
models, groups them into releases, applies release-family behavior, sorts the
result, builds a template context, and renders the complete Markdown document.

Writing the file is a final boundary. Most of the subsystem works with domain
objects or an in-memory string before the requested `CHANGELOG.md` path is
replaced.

---

<Section title="Subsystem Responsibilities">

<Grid columns={2}>

<ArchitectureCard title="Configuration" subtitle="config/" icon={<Icon name="configuration" />}>

`ChangelogConfigResolver` reads the project-owned changelog tables into typed
dataclasses used by providers, processors, links, metadata, and rendering.

</ArchitectureCard>

<ArchitectureCard title="Providers" subtitle="providers/" icon={<Icon name="dependency" />}>

`MessageProviderFactory` always creates the Git provider and conditionally
adds the pending-commit provider when that active configuration is enabled.

</ArchitectureCard>

<ArchitectureCard title="Processing" subtitle="processing/" icon={<Icon name="pipeline" />}>

`ChangelogProcessingPipeline` cleans, parses, expands, filters, ignores,
deduplicates, and groups messages into semantic commit structures.

</ArchitectureCard>

<ArchitectureCard title="Release Planning" subtitle="generator.py and sorting/" icon={<Icon name="release" />}>

The generator chooses stable or prerelease range boundaries, records promoted
prerelease tags, applies release behavior, and sorts release objects.

</ArchitectureCard>

<ArchitectureCard title="Domain Models" subtitle="models/" icon={<Icon name="architecture" />}>

Commit, group, scope, section, release, metadata, contributor, statistics, and
template models carry data without embedding Git or file-writing behavior.

</ArchitectureCard>

<ArchitectureCard title="Rendering" subtitle="rendering/" icon={<Icon name="template" />}>

`JinjaRenderer` turns the final `Changelog` model and rendering configuration
into one Markdown string using the initialized project template.

</ArchitectureCard>

</Grid>

</Section>

---

<WorkflowDiagram
    title="Active Generation Flow"
    timeline={[
        {title: "Enter through command or pipeline", description: "The changelog command or GenerateChangelogStep delegates eligibility and execution to WorkflowEngine.", status: "completed"},
        {title: "Check whether generation applies", description: "WorkflowEngine evaluates skip, enablement, release state, and dry-run conditions before calling the generator.", status: "completed"},
        {title: "Load services and configuration", description: "ChangelogGenerator creates its typed configuration, GitService, processing pipeline, release processor, sorter, and Jinja renderer.", status: "completed"},
        {title: "Plan release ranges", description: "Existing tags determine unreleased, prerelease, stable, and promoted-prerelease boundaries.", status: "current"},
        {title: "Collect raw messages", description: "GitProvider reads repository history; PendingCommitProvider may add the latest pending template content.", status: "pending"},
        {title: "Build the domain model", description: "The processing pipeline creates semantic commit groups, and the generator assembles release metadata and comparisons.", status: "pending"},
        {title: "Apply release behavior and order", description: "ReleaseBehaviorProcessor handles release-family policy and DefaultReleaseSorter orders the final releases.", status: "pending"},
        {title: "Render and write", description: "JinjaRenderer produces Markdown; the generator writes only after successful collection, processing, and rendering.", status: "pending"},
    ]}
/>

---

<Section title="Provider Boundary">

<DependencyCard
    title="Message Sources"
    dependencies={[
        {source: "MessageProviderFactory", target: "GitProvider", description: "Always supplies committed messages for each planned Git range."},
        {source: "MessageProviderFactory", target: "PendingCommitProvider", description: "Optionally supplies the pending commit template for the latest unreleased range."},
        {source: "Providers", target: "list[str]", description: "Return raw messages in provider order without deciding presentation."},
        {source: "ChangelogProcessingPipeline", target: "Commit models", description: "Owns normalization, semantic parsing, filtering, deduplication, and grouping."},
    ]}
/>

The provider protocol keeps collection separate from interpretation. This
makes it possible to add another source without teaching the renderer how that
source stores messages.

</Section>

---

<Section title="Release and Range Model">

- The newest tag is the lower boundary for the unreleased `HEAD` range.
- A prerelease uses the adjacent older tag as its lower boundary.
- A stable release searches for the previous stable tag so its range covers
  the full prerelease family promoted into that stable version.
- Release objects can carry dates, comparison links, promoted tags, metadata,
  contributors, statistics, and grouped semantic changes.
- `ReleaseBehaviorProcessor` applies configured stable, prerelease, and
  unreleased presentation policy before final sorting.

This planning belongs to the generator because it depends on relationships
between several tags and releases, not on a single message provider.

</Section>

---

<Section title="Configuration and Integration Status">

<InfoBox variant="warning" title="Loaded configuration is not always active behavior">

The active `generator.py` creates `GitProvider` and, when enabled,
`PendingCommitProvider`. It does not currently invoke
`PendingCommitIntegrator`, even though integration modules and generation or
placement settings exist in the source. Treat those related properties as
loaded-only unless the configuration reference marks a specific property
active.

</InfoBox>

Backup-looking generator copies, debug checkpoints, and files under `old/` are
not part of this documented path. The architecture describes the active
`app/core/changelog/generator.py` implementation.

</Section>

---

<Section title="Failure and Write Boundaries">

- Git range or provider failures stop generation before the final write.
- Parsing and rendering happen before the destination file is replaced.
- The flow is not a transaction across the larger release pipeline; an
  earlier workflow step may already have changed project files.
- Dry-run prevents the changelog file write, but callers should still expect
  configuration resolution, repository reads, and planning work.
- A rendering or template error is surfaced rather than producing a partial
  Markdown document.

</Section>

---

<Section title="Continue">

<RelatedCommands
    commands={[
        {title: "Generate Changelog", href: "../commands/changelog/generate/overview", description: "Use the public command and review its options, output, examples, and safety."},
        {title: "Changelog Configuration", href: "../configuration/changelog/overview", description: "Navigate every changelog table, property, and implementation status."},
        {title: "Workflow Engine", href: "./workflow-engine", description: "See the eligibility check and surrounding release phases."},
        {title: "Changelog Troubleshooting", href: "../commands/changelog/generate/troubleshooting", description: "Diagnose history, template, parsing, and output problems."},
    ]}
/>

</Section>
