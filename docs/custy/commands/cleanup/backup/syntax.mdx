---
title: "Syntax"
description: "Command syntax for removing obsolete backup files."
sidebar_position: 3
sidebar_key: custy-cleanup-backup

tags:
  - cleanup
  - backup
  - syntax

keywords:
  - custy cleanup backups syntax
  - cleanup backup command
---

import {
  CommandHero,
  CommandSyntax,
  Section,
  InfoBox,
  Terminal,
} from "@site/src/components/docs";

<CommandHero
  command="custy cleanup backups"
  title="Syntax"
  description="Learn how to remove obsolete backup files using Custy's Backup Cleanup command."
  category="Maintenance"
  status="Recommended"
  syntax="custy cleanup backups"
  tags={["cleanup", "backup", "syntax"]}
  links={[
    {
      label: "Requirements",
      href: "./requirements",
    },
    {
      label: "Options",
      href: "./options",
    },
    {
      label: "Examples",
      href: "./examples",
    },
  ]}
/>

## Overview

Backup Cleanup removes obsolete backup files from your project's backup
directories.

The command supports cleaning:

- commit message backups,
- tag message backups,
- or both.

The cleanup behavior is determined by command-line options and project
configuration.

---

<Section
    title="Basic Syntax"
>

<CommandSyntax
  examples={[
    {
      title: "Backup Cleanup",
      description:
        "Remove obsolete backup files using the configured cleanup settings.",
      command: "custy cleanup backups",
    },
  ]}
/>

</Section>

---

<InfoBox variant="note" title="Public invocation">

Use `custy cleanup backups`. Backup cleanup is reused internally by larger
profiles, but `cleanup_backups` is not accepted as a public Run argument.

</InfoBox>

---

<Section
    title="Cleanup Targets"
>

Depending on the selected cleanup type, Custy may remove:

- commit message backups,
- tag message backups,
- both backup categories.

The cleanup target can be selected explicitly or resolved from your project's
configuration.

</Section>

---

<Section
    title="Global Options"
>

Like every Custy command, Backup Cleanup supports the global CLI options.

Examples include:

<Terminal>

```bash
custy --dry-run cleanup backups
```

</Terminal>

<Terminal>

```bash
custy --debug cleanup backups
```

</Terminal>

<Terminal>

```bash
custy --log-level DEBUG cleanup backups
```

</Terminal>

Global options are always specified **before** the command name.

</Section>

---

<InfoBox
    variant="tip"
    title="Configuration First"
>

When cleanup options are not supplied on the command line, Custy resolves the
effective cleanup configuration from your project's `config.toml`.

This allows projects to define consistent cleanup behavior while still allowing
temporary command-line overrides when needed.

</InfoBox>

---

<Section
    title="Exit Behavior"
>

Backup Cleanup finishes after processing the configured backup directories.

Depending on the current project state, the command may:

- remove obsolete backup files,
- determine that no cleanup is necessary,
- report the cleanup summary.

Successful completion does not modify your Git repository.

</Section>

---

<Section
    title="What's Next?"
>

Continue to the **Options** page to learn how cleanup targets and retention
behavior can be customized for individual executions.

</Section>
