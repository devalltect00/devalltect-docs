---
title: "Requirements"
description: "Prerequisites for using Custy's Branch Cleanup command."
sidebar_position: 2
sidebar_key: custy-cleanup-branch

tags:
    - cleanup
    - branch
    - requirements

keywords:
    - custy cleanup branches
    - branch cleanup requirements
---

import {
    CommandHero,
    RequirementGrid,
    Section,
    InfoBox,
    Icon,
} from "@site/src/components/docs";

<CommandHero
    command="custy cleanup branches"
    title="Requirements"
    description="Understand the prerequisites before removing obsolete Git branches."
    category="Maintenance"
    status="Advanced"
    syntax="custy cleanup branches"
    tags={[
        "cleanup",
        "branch",
        "requirements",
    ]}
    links={[
        {
            label: "Overview",
            href: "./overview",
        },
        {
            label: "Configuration",
            href: "./configuration",
        },
        {
            label: "Workflow",
            href: "./workflow",
        },
    ]}
/>

## Overview

Branch Cleanup operates directly on your project's Git repository.

Before running the command, ensure that your repository and project
configuration are ready for branch maintenance.

---

<InfoBox
    variant="warning"
    title="Review Before Deleting"
>

Branch Cleanup permanently removes local Git branches.

Always review the configured cleanup filters before executing the command.

</InfoBox>

---

<Section
    title="Prerequisites"
>

<RequirementGrid
    title="Required Environment"
    items={[
        {
            title: "Git Repository",
            icon: <Icon name="git" />,
            children: (
                <>
                    Branch Cleanup requires the current directory to be a
                    valid Git repository.
                </>
            ),
        },
        {
            title: "Project Configuration",
            icon: <Icon name="configuration" />,
            children: (
                <>
                    Cleanup behavior is resolved from your project's
                    <code>config.toml</code>.
                </>
            ),
        },
        {
            title: "Configured Cleanup Rules",
            icon: <Icon name="table" />,
            children: (
                <>
                    Configure branch prefixes and optional filters before
                    attempting branch cleanup.
                </>
            ),
        },
        {
            title: "Repository Access",
            icon: <Icon name="security" />,
            children: (
                <>
                    Custy must be able to inspect and remove local Git
                    branches.
                </>
            ),
        },
    ]}
/>

</Section>

---

<Section
    title="Recommended Preparation"
>

Before running Branch Cleanup:

- Review the configured branch prefixes.
- Verify which branches are expected to match.
- Confirm that obsolete branches are no longer needed.
- Review merge status and branch age filters.

These checks help ensure that only the intended branches become eligible for
cleanup.

</Section>

---

<InfoBox
    variant="tip"
    title="Nothing Happens by Default"
>

A newly initialized Custy project uses:

```toml
include_prefixes = []
```

This means Branch Cleanup selects no branches until you explicitly configure
which branch prefixes should be considered.

This conservative default helps prevent accidental branch removal.

</InfoBox>

---

<Section
    title="Protected Branches"
>

Custy always protects important branches.

By default, the following branches are never removed:

- `main`
- `master`
- `develop`

These protections apply even if a branch matches the configured cleanup
filters.

</Section>

---

<Section
    title="What Is Not Required?"
>

Branch Cleanup does **not** require:

- staged changes,
- commits waiting to be pushed,
- release tags,
- workflow execution.

The command focuses exclusively on evaluating and removing eligible local Git
branches.

</Section>

---

<Section
    title="What's Next?"
>

Continue to the **Syntax** page to learn how to execute Branch Cleanup directly
or as an individual workflow stage.

</Section>
