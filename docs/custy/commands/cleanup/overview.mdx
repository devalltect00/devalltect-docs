---
title: "Overview"
description: "Remove obsolete backup files and Git branches using Custy's cleanup command."
sidebar_position: 1
sidebar_key: custy-commands-cleanup

tags:
  - cleanup
  - maintenance
  - git
  - backups

keywords:
  - custy cleanup
  - cleanup backups
  - cleanup branches
---

import {
  CommandHero,
  CommandMetadata,
  FeatureGrid,
  RelatedCommands,
  Section,
  InfoBox,
  Screenshot,
  Icon,
} from "@site/src/components/docs";

<CommandHero
  command="custy cleanup"
  title="Clean Up Project Resources"
  description="Remove obsolete backup files and stale Git branches while keeping your project organized and maintainable."
  category="Maintenance"
  status="Recommended"
  syntax="custy cleanup <subcommand>"
  tags={["maintenance", "cleanup", "git", "backups"]}
  links={[
    {
      label: "Backup Cleanup",
      href: "./backup/overview",
    },
    {
      label: "Branch Cleanup",
      href: "./branch/overview",
    },
    {
      label: "Run Profiles",
      href: "../run/overview",
    },
  ]}
/>

<CommandMetadata
  metadata={[
    {
      label: "Category",
      value: "Maintenance",
    },
    {
      label: "Git Repository",
      value: "Required for branch cleanup",
    },
    {
      label: "Configuration",
      value: "config.toml",
    },
    {
      label: "Workflow Support",
      value: "Partial",
    },
  ]}
/>

## Overview

The **cleanup** command removes resources that are no longer needed after
regular development activities.

Custy currently supports two independent cleanup operations:

- **Backup Cleanup** — removes older backup files while preserving the newest
  backups according to the configured retention policy.

- **Branch Cleanup** — removes Git branches that match user-defined cleanup
  rules while protecting important branches such as `main`, `master`, and
  `develop`.

Although both operations belong to the same command family, they serve
different purposes and have different safety characteristics.

---

<InfoBox
    variant="tip"
    title="Two Independent Cleanup Operations"
>

Backup cleanup manages files generated by Custy.

Branch cleanup manages Git branches in your repository.

These operations are independent and may be executed separately depending on
your maintenance needs.

</InfoBox>

---

<Section
    title="Cleanup Types"
>

<FeatureGrid
  title="Available Cleanup Operations"
  items={[
    {
      title: "Backup Cleanup",
      icon: <Icon name="copy" />,
      children: (
        <>
          Removes older backup files while keeping the most recent backups according to
          the configured retention policy.
        </>
      ),
    },
    {
      title: "Branch Cleanup",
      icon: <Icon name="git" />,
      children: (
        <>
          Removes stale Git branches that satisfy the configured cleanup filters while
          protecting important branches.
        </>
      ),
    },
  ]}
/>

</Section>

---

<Section
    title="Automatic vs Manual Cleanup"
>

| Cleanup Operation | Automatic | Manual | Purpose                       |
| ----------------- | --------- | ------ | ----------------------------- |
| Backup Cleanup    | ✅ Yes    | ✅ Yes | Remove old backup files.      |
| Branch Cleanup    | ❌ No     | ✅ Yes | Remove obsolete Git branches. |

Backup cleanup may execute automatically during certain Custy workflows.

Branch cleanup is intentionally performed manually because it modifies the Git
repository.

</Section>

---

<InfoBox
    variant="warning"
    title="Review the Selected Cleanup Operation"
>

Backup Cleanup removes obsolete backup files created by Custy.

Branch Cleanup removes eligible Git branches from your repository.

Although both belong to the Cleanup command family, they operate on different
resources and should be used with an understanding of their respective
configuration and safety mechanisms.

</InfoBox>

---

<Section
    title="Workflow Integration"
>

Some cleanup operations can participate in workflow pipelines, while others
are intentionally manual because they modify Git resources.

Backup cleanup is integrated into several Custy workflow pipelines and helps
keep backup directories organized automatically.

Branch cleanup is intentionally excluded from automated release workflows and
must be executed manually to avoid accidental branch removal.

</Section>

---

<Section
    title="Configuration Driven"
>

Cleanup behavior is primarily controlled through your project's
`config.toml`.

Examples of configurable behavior include:

- Backup retention count.
- Branch name prefixes.
- Merge status filters.
- Branch age filters.
- Branch date filters.

This allows each project to define maintenance rules that match its own Git
workflow.

</Section>

---

<RelatedCommands
  commands={[
    {
      name: "init",
      description: "Generate Custy configuration files used by cleanup.",
      href: "../init/overview",
    },
    {
      name: "backup",
      description: "Create backup files before they are cleaned up.",
      href: "../backup/overview",
    },
    {
      name: "run",
      description: "Execute workflow pipelines that include automatic backup cleanup.",
      href: "../run/overview",
    },
  ]}
/>
