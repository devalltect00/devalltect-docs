---
title: "Overview"
description: "Prepare an existing project for use with Custy by generating project-specific configuration, templates, and supporting resources."
sidebar_position: 1
sidebar_key: custy-commands-init

tags:
  - initialization
  - setup
  - configuration
  - templates

keywords:
  - custy init
  - project initialization
  - setup custy
  - configuration
  - templates
---

import {
  CommandHero,
  CommandMetadata,
  FeatureGrid,
  FeatureCard,
  InfoBox,
  RelatedCommands,
  Section,
  Terminal,
  Icon,
} from "@site/src/components/docs";

<CommandHero
  command="custy init"
  title="Initialize a Project"
  description="Prepare an existing project for use with Custy by generating project-specific configuration, templates, and supporting resources."
  category="Initialization"
  status="Recommended"
  syntax="custy init"
  tags={["configuration", "templates", "project setup"]}
  links={[
    {
      label: "Syntax",
      href: "./syntax",
    },
    {
      label: "Examples",
      href: "./examples",
    },
    {
      label: "Workflow",
      href: "./workflow",
    },
  ]}
/>

<CommandMetadata
  items={[
    {
      label: "Category",
      value: "Initialization",
      icon: "directory",
    },
    {
      label: "Complexity",
      value: "Beginner",
      icon: "requirement",
    },
    {
      label: "Modifies Git History",
      value: "No",
      icon: "git",
    },
    {
      label: "Typical Duration",
      value: "Less than a minute",
      icon: "timeline",
    },
  ]}
/>

## Overview

The `custy init` command prepares an existing project to work with **Custy**.

Rather than changing your project's Git history or executing release workflows,
this command creates a configurable project workspace under
`.config/custy/`. These generated files allow you to customize Custy's
behavior without modifying the application itself.

Running `custy init` is the recommended first step when adopting Custy for a
new or existing repository.

<InfoBox
    variant="tip"
    title="Recommended"
>

Although Custy provides sensible built-in defaults and many commands can work
without project-specific configuration, running `custy init` is highly
recommended because it creates customizable configuration and template files
that can be tailored to your project's workflow.

</InfoBox>

---

<Section
    title="What This Command Does"
>

<FeatureGrid
  title="What This Command Does"
  items={[
    {
      title: "Generate Configuration",
      icon: <Icon name="configuration" />,
      children: (
        <>
          Creates the project's primary configuration file under
          <code>.config/custy/config.toml</code>
        </>
      ),
    },
    {
      title: "Create Templates",
      icon: <Icon name="file" />,
      children: (
        <>
          Generates default commit, tag, and changelog templates that can be customized
          for your team's workflow.
        </>
      ),
    },
    {
      title: "Prepare Project",
      icon: <Icon name="directory" />,
      children: <>Creates the required directory structure used by Custy.</>,
    },
    {
      title: "Optional Examples",
      icon: <Icon name="project" />,
      children: <>Generates example resources demonstrating supported templates.</>,
    },
  ]}
/>

</Section>

---

<Section
    title="When Should You Use It?"
>

You should normally run `custy init` when:

- Setting up Custy in a project for the first time.
- Adding Custy to an existing Git repository.
- Creating project-specific configuration.
- Regenerating default templates after upgrading Custy.
- Preparing a repository for team-wide usage.

In most projects, `custy init` is executed once during the initial setup.
Afterward, the generated configuration can simply be updated as your project
evolves.

</Section>

---

<Section
    title="What This Command Does Not Do"
>

Unlike workflow commands, `custy init` does **not**:

- Create commits.
- Create Git tags.
- Push changes to remote repositories.
- Generate releases.
- Modify Git history.
- Execute release pipelines.

Its sole responsibility is preparing your project for future Custy workflows.

</Section>

---

<Section
    title="Project Workspace"
>

After initialization, your project contains a dedicated Custy workspace.

<Terminal>

{`your-project/
└── .config/
    └── custy/
        ├── config.toml
        └── templates/
            ├── backups/
            ├── changelog/
            ├── examples/
            ├── commit-message.txt
            └── tag-message.txt`}

</Terminal>

This workspace keeps project-specific configuration separate from Custy's
internal defaults, making it easier to customize behavior without modifying
the application itself.

</Section>

---

<Section
    title="Why Initialize a Project?"
>

Custy ships with sensible default behavior.

This means initialization is **recommended**, but not strictly required.

Without running `custy init`, Custy falls back to its built-in defaults.

Running `custy init` gives you additional flexibility by allowing your project
to define its own:

- configuration
- templates
- preferences
- directory structure

This approach provides a balance between convenience and customization.

</Section>

---

<InfoBox
    variant="note"
    title="Customization"
>

Because the generated files belong to your project rather than Custy itself,
they can be reviewed, version-controlled, and shared with other contributors
using the same repository.

</InfoBox>

---

<Section
    title="What's Next?"
>

Once your project has been initialized, the next step depends on your workflow.

For example:

- Validate your project configuration.
- Start using individual Custy commands.
- Execute predefined workflows.
- Customize the generated configuration and templates.

<RelatedCommands
  commands={[
    {
      title: "Validate",
      href: "../validate/overview",
      description: "Verify your project's Custy configuration.",
    },
    {
      title: "Run",
      href: "../run/overview",
      description: "Execute predefined workflows.",
    },
    {
      title: "Workflow",
      href: "../workflow/overview",
      description: "Learn about workflow orchestration.",
    },
    {
      title: "Version",
      href: "../version/overview",
      description: "Manage project versions.",
    },
  ]}
/>

</Section>
