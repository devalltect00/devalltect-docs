---
title: "Init Troubleshooting"
description: "Resolve common issues encountered while initializing a project with Custy."
sidebar_position: 10
sidebar_key: custy-commands-init-troubleshooting

tags:
  - troubleshooting
  - initialization
  - errors

keywords:
  - custy init troubleshooting
  - initialization errors
  - common problems
---

import { CommandHero, InfoBox, Section } from "@site/src/components/docs";

<CommandHero
  command="custy init"
  title="Init Troubleshooting"
  description="Common problems, possible causes, and recommended solutions."
  category="Initialization"
  status="Support Guide"
  syntax="custy init"
  tags={["troubleshooting", "errors", "support"]}
  links={[
    {
      label: "Troubleshooting Hub",
      href: "../../troubleshooting/overview",
    },
    {
      label: "Requirements",
      href: "./requirements",
    },
    {
      label: "Configuration",
      href: "./configuration",
    },
    {
      label: "FAQ",
      href: "./faq",
    },
  ]}
/>

## Overview

Most initialization problems are related to the project environment rather than
Custy itself.

This page lists the most common situations and the recommended actions for
resolving them.

<InfoBox
    variant="tip"
    title="Start Here"
>

Before investigating an error, confirm that you are running the command from
the root directory of your project and that Custy has permission to create
files within the repository.

</InfoBox>

---

## Configuration Already Exists

<Section compact>

**What you see**

Custy reports that configuration files or templates already exist.

**Why it happens**

The project has already been initialized or previously contains generated
Custy resources.

**How to resolve it**

- Review the existing files before replacing them.
- Use `--ask` if you want confirmation before overwriting.
- Use `--force` only when you intentionally want to regenerate resources.

</Section>

---

## Permission Denied

<Section compact>

**What you see**

Initialization fails while creating files or directories.

**Why it happens**

The current user does not have permission to write to the project directory.

**How to resolve it**

- Verify that the project directory is writable.
- Close applications that may be locking generated files.
- Run the command from a directory where write access is available.

</Section>

---

## Unexpected Configuration

<Section compact>

**What you see**

Custy behaves differently than expected after initialization.

**Why it happens**

The project already contains a customized
`.config/custy/config.toml` configuration.

**How to resolve it**

- Review the generated configuration.
- Verify initialization options.
- Compare the configuration with your team's expected defaults.

</Section>

---

## Examples Were Not Generated

<Section compact>

**What you see**

The `examples` directory is missing, or its nested commit/tag example folders
do not contain the currently packaged examples.

**Why it happens**

Initialization was performed using a mode that does not generate examples, or
the project contains an older/partial example tree that predates newer
packaged resources.

**How to resolve it**

Run:

```text
custy init --mode examples
```

or perform a full initialization:

```text
custy init
```

Without `--force`, Custy recursively adds missing example directories and
files while preserving existing example files. Use `--force` only when you
intend to refresh and overwrite the existing example files as well.

</Section>

---

<InfoBox
    variant="note"
    title="Next Diagnostic Step"
>

If the issue persists, review the generated configuration and initialization
mode before attempting the command again.

Most initialization issues are resolved by correcting the selected mode or
overwrite behavior.

</InfoBox>

---

## Continue

<Section compact>

If your question is not covered here, continue to the **FAQ** page for answers
to frequently asked questions about project initialization.

</Section>
