---
title: "Syntax"
description: "Supported custy tag command forms."
sidebar_position: 3
sidebar_key: custy-tag-syntax
tags: [tag, syntax, cli]
keywords: [custy tag syntax]
---

import {
    CommandHero,
    CommandSyntax,
    InfoBox,
    Section,
} from "@site/src/components/docs";

<CommandHero
    command="custy tag"
    title="Command Syntax"
    description="Use the direct tag command for version and message overrides or select the registered profile through Run."
    category="Git Operations"
    status="Stable"
    syntax="custy tag [OPTIONS]"
    tags={[
        "syntax",
        "CLI",
        "version tag",
    ]}
    links={[
        {
            label: "Overview",
            href: "./overview",
        },
        {
            label: "Options",
            href: "./options",
        },
        {
            label: "Examples",
            href: "./examples",
        },
    ]}
/>

## Overview

The direct form exposes the complete version, tag-message, modifier, force, and
backup option surface. Place global dry-run before `tag`.

---

<Section
    title="Supported Command Forms"
>

<CommandSyntax
    examples={[
        {title: "Use project version settings", command: "custy tag", description: "Resolve and create the tag through the configured strategy."},
        {title: "Provide an exact tag", command: "custy tag --tag v1.2.3", description: "Use a manually selected tag value."},
        {title: "Select PEP 440", command: "custy tag --strategy pep440", description: "Generate a PEP 440 version from repository state."},
        {title: "Create a pre-release label", command: "custy tag --tag v1.3.0 --pre beta", description: "Add a pre-release label to the resolved version."},
        {title: "Preview tag preparation", command: "custy --dry-run tag --tag v1.2.3", description: "Inspect the planned tag profile without normal mutations."},
    ]}
/>

</Section>

---

<Section
    title="Direct Command and Run Profile"
>

`custy run tag` selects the registered tag profile but does not expose all
tag-specific arguments. Use the direct form for version and message overrides.

<InfoBox
    variant="tip"
    title="Prefer explicit versions for manual releases"
>

When you already know the intended version, `--tag` makes the release target
clear and easier to review.

</InfoBox>

</Section>
