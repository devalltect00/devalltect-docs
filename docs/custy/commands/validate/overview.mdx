---
title: "Overview"
description: "Verify that your project is ready before executing Custy workflows."
sidebar_position: 1
sidebar_key: custy-commands-validate
---

import {
  CommandHero,
  CommandMetadata,
  FeatureGrid,
  RelatedCommands,
  Section,
  InfoBox,
  Icon,
} from "@site/src/components/docs";

<CommandHero
  command="custy validate"
  title="Validate a Project"
  description="Verify that your project satisfies the prerequisites required before executing Custy workflows."
  category="Validation"
  status="Recommended"
  syntax="custy validate"
  tags={["validation", "git", "workflow", "pre-flight"]}
  links={[
    {
      label: "Requirements",
      href: "./requirements",
    },
    {
      label: "Syntax",
      href: "./syntax",
    },
    {
      label: "Workflow",
      href: "./workflow",
    },
  ]}
/>

<CommandMetadata
  items={[
    {
      label: "Category",
      value: "Validation",
    },
    {
      label: "Available As",
      value: "custy validate",
    },
    {
      label: "Modifies Repository",
      value: "No",
    },
    {
      label: "Recommended",
      value: "Before commit, tag, push, release, and workflow commands.",
    },
  ]}
/>

## Overview

The **validate** command performs a series of pre-flight checks to verify that
your project is ready for subsequent Custy operations.

Rather than modifying your repository, this command inspects your project,
configuration, Git repository, and required resources to detect problems before
they interrupt a larger workflow.

<InfoBox
    variant="tip"
    title="Automatic Validation"
>

Although this command can be executed manually, most workflow-oriented commands
such as `custy run` automatically perform validation before executing the
remaining workflow steps.

</InfoBox>

---

<Section
    title="What This Command Does"
>

<FeatureGrid
  title="Validation Categories"
  items={[
    {
      title: "Git Repository",
      icon: <Icon name="git" />,
      children: <>Confirms that the current directory is a valid Git repository.</>,
    },
    {
      title: "Project Configuration",
      icon: <Icon name="configuration" />,
      children: (
        <>
          Resolves configuration values and verifies that required project resources
          exist.
        </>
      ),
    },
    {
      title: "Git State",
      icon: <Icon name="success" />,
      children: (
        <>
          Verifies staged changes and repository readiness before commit-related
          workflows.
        </>
      ),
    },
    {
      title: "Workflow Resources",
      icon: <Icon name="directory" />,
      children: (
        <>
          Checks required version files, commit templates, tag templates, and other
          workflow resources.
        </>
      ),
    },
  ]}
/>

</Section>

---

<Section
    title="Why Validation Matters"
>

Running validation before a workflow helps detect common problems early.

Examples include:

- The current directory is not a Git repository.
- A required Git remote has not been configured.
- The configured version file cannot be found.
- Required template files are missing.
- No staged changes are available before a commit workflow.
- Commit message validation does not satisfy the selected strategy.

By detecting these issues first, Custy prevents incomplete or failed workflows.

</Section>

---

<InfoBox
    variant="note"
    title="Configuration Driven"
>

Most validation targets are resolved from your project's
`config.toml` configuration.

For example, the version file location, template locations, and Git remote name
are determined from the project configuration instead of fixed paths whenever
possible.

</InfoBox>

---

<Section
    title="When Should I Run Validation?"
>

Validation is useful whenever you want to confirm that your project is ready
before performing Git or release-related operations.

Typical situations include:

- Before creating a commit.
- Before generating a version.
- Before creating a Git tag.
- Before pushing changes.
- Before running a release workflow.

If you normally use `custy run`, these checks are executed automatically as part
of the workflow.

</Section>

---

<RelatedCommands
  commands={[
    {
      name: "init",
      description: "Generate the configuration and templates required by validation.",
      href: "../init/overview",
    },
    {
      name: "run",
      description: "Execute workflow pipelines that automatically include validation.",
      href: "../run/overview",
    },
    {
      name: "workflow",
      description: "Learn how validation fits into Custy's workflow engine.",
      href: "../workflow/overview",
    },
  ]}
/>
