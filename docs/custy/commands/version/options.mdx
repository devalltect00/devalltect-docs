---
title: "Options"
description: "Available command-line options for Version Update."
sidebar_position: 4
sidebar_key: custy-version

tags:
    - version
    - options

keywords:
    - custy version options
---

import {
    CommandHero,
    CommandOption,
    Section,
    InfoBox,
} from "@site/src/components/docs";

<CommandHero
    command="custy version"
    title="Options"
    description="Customize how Custy generates and applies project version updates."
    category="Versioning"
    status="Stable"
    syntax="custy version update"
    tags={[
        "options",
        "version",
    ]}
    links={[
        {
            label: "Syntax",
            href: "./syntax",
        },
        {
            label: "Configuration",
            href: "./configuration",
        },
        {
            label: "Versioning Strategies",
            href: "./versioning-strategies",
        },
    ]}
/>

## Overview

Version Update provides command-line options for selecting the versioning
strategy, controlling how the next version is generated, and overriding the
default project configuration.

When an option is omitted, Custy resolves its value from the project
configuration before falling back to built-in defaults.

---

<Section
    title="Available Options"
>

<CommandOption
    options={[
        {
            name: "--strategy",
            required: false,
            defaultValue: "config.toml",
            description:
                "Select the versioning strategy used to generate the next version.",
            example:
                "--strategy semver",
        },
        {
            name: "--bump",
            required: false,
            defaultValue: "auto",
            description:
                "Specify the version bump level such as auto, patch, minor, or major.",
            example:
                "--bump minor",
        },
        {
            name: "--tag",
            required: false,
            description:
                "Provide an explicit version instead of generating one from a strategy.",
            example:
                "--tag v2.4.0",
        },
        {
            name: "--pre-release",
            required: false,
            description:
                "Generate a pre-release version such as alpha, beta, or rc.",
            example:
                "--pre-release beta",
        },
        {
            name: "--post-release",
            required: false,
            defaultValue: "false",
            description:
                "Generate a post-release version when supported by the selected strategy.",
        },
        {
            name: "--dev-release",
            required: false,
            defaultValue: "false",
            description:
                "Generate a development release when supported by the selected strategy.",
        },
        {
            name: "--meta",
            required: false,
            description:
                "Append build metadata or local version information.",
            example:
                "--meta build.42",
        },
        {
            name: "--epoch",
            required: false,
            description:
                "Specify the PEP 440 epoch component.",
            example:
                "--epoch 1",
        },
    ]}
/>

</Section>

---

<Section
    title="Global Options"
>

Like every Custy command, Version Update also supports the global CLI options.

Examples include:

- `--dry-run`
- `--debug`
- `--log-level`

Global options always appear before the command.

Example:

```bash
custy --dry-run version update
```

</Section>

---

<InfoBox
    variant="tip"
    title="Configuration First"
>

Most projects define their preferred versioning strategy inside
`config.toml`.

Command-line options are useful when temporarily overriding those defaults for
a single execution.

</InfoBox>

---

<Section
    title="Option Resolution"
>

When multiple sources provide the same value, Custy resolves them using the
following priority:

1. Command-line arguments.
2. Project configuration (`config.toml`).
3. Built-in defaults.

This allows project-wide defaults while still supporting temporary overrides
for individual executions.

</Section>

---

<Section
    title="What's Next?"
>

Continue to the **Configuration** page to learn how to configure default
versioning behavior for your project.

</Section>
