---
title: "Commands, Pipelines, and Workflows"
description: "Understand how focused CLI commands, ordered pipeline profiles, and Custy's experimental workflow command differ."
sidebar_position: 1
sidebar_key: custy-concepts-commands-pipelines-workflows

tags:
  - custy
  - commands
  - pipelines
  - workflows

keywords:
  - custy command model
  - custy pipeline profiles
  - custy workflows
---

import {
    CommandHero,
    CommandMetadata,
    FeatureGrid,
    Icon,
    InfoBox,
    RelatedCommands,
    Section,
    Terminal,
    WorkflowTimeline,
} from "@site/src/components/docs";

<CommandHero
    command="custy COMMAND | custy run PROFILE"
    title="Commands, Pipelines, and Workflows"
    description="Choose the smallest Custy execution model that reaches the outcome you need."
    category="Core Concepts"
    status="Essential"
    syntax="custy COMMAND [OPTIONS]"
    tags={[
        "CLI model",
        "pipeline resolution",
        "workflow policy",
    ]}
    links={[
        {label: "Command Reference", href: "../commands/overview"},
        {label: "Run Profiles", href: "../commands/run/profiles"},
        {label: "Pipeline Guide", href: "../pipelines/overview"},
        {label: "Safety Model", href: "./safety-and-side-effects"},
    ]}
/>

<CommandMetadata
    items={[
        {label: "Command", value: "One focused entry point", icon: "command"},
        {label: "Pipeline", value: "Ordered registered steps", icon: "pipeline"},
        {label: "Workflow", value: "Policy and lifecycle", icon: "workflow"},
        {label: "Workflow CLI", value: "Experimental", icon: "warning"},
    ]}
/>

## Overview

Custy exposes focused commands and reusable pipeline profiles through the same
CLI. The terms describe different layers: a **command** is what you invoke, a
**pipeline** is an ordered execution plan, and a **workflow** describes the
broader release policy or lifecycle that coordinates work.

<InfoBox
    variant="info"
    title="The practical rule"
>

Use a direct command for one focused operation. Use `custy run` when you want a
supported profile to coordinate several registered steps in a predictable
order.

</InfoBox>

---

<Section
    title="Three Different Layers"
    subtitle="Keeping these layers separate makes command behavior and side effects easier to predict."
>

<FeatureGrid
    title="Execution Model"
    items={[
        {
            title: "Command",
            icon: <Icon name="terminal" />,
            children: <>A public CLI entry point such as <code>custy validate</code>, <code>custy commit</code>, or <code>custy changelog generate</code>.</>,
        },
        {
            title: "Pipeline",
            icon: <Icon name="pipeline" />,
            children: <>A resolved, globally ordered list of registered steps. Public profile names are expanded before execution begins.</>,
        },
        {
            title: "Workflow",
            icon: <Icon name="workflow" />,
            children: <>The policy and lifecycle surrounding preparation, review, release, and finalization. It is broader than one command or profile.</>,
        },
    ]}
/>

</Section>

---

<Section
    title="How a Run Profile Becomes a Pipeline"
>

<WorkflowTimeline
    title="Pipeline Resolution"
    items={[
        {
            title: "Select public profiles",
            description: <>Pass one or more supported names to <code>custy run</code>.</>,
            status: "current",
        },
        {
            title: "Expand profile definitions",
            description: "Custy replaces each profile name with its configured internal step list.",
            status: "pending",
        },
        {
            title: "Merge and deduplicate",
            description: "When profiles overlap, repeated steps are included only once.",
            status: "pending",
        },
        {
            title: "Apply global ordering",
            description: "The resolver sorts the combined steps into Custy's required execution order.",
            status: "pending",
        },
        {
            title: "Execute sequentially",
            description: "The pipeline builds registered step objects and runs them one after another.",
            status: "pending",
        },
    ]}
/>

</Section>

---

<Section
    title="Public Run Profiles"
>

The current public `custy run` argument accepts six profile names:

<FeatureGrid
    title="Supported Profiles"
    items={[
        {title: "Commit", icon: <Icon name="git" />, children: "Prepare project artifacts and create a local commit."},
        {title: "Tag", icon: <Icon name="release" />, children: "Prepare version state and create a local tag."},
        {title: "Push", icon: <Icon name="network" />, children: "Synchronize HEAD and an optional tag with resolved remotes."},
        {title: "Dev", icon: <Icon name="play" />, children: "Validate and review the commit message, create a commit, and push branch HEAD without release side effects."},
        {title: "Release", icon: <Icon name="pipeline" />, children: "Apply a version, commit, tag, and push a release."},
        {title: "Full", icon: <Icon name="workflow" />, children: "Run Release and then invoke experimental finalization."},
    ]}
/>

Internal resolver profiles are implementation details. A profile appearing in
source code or broad help text does not mean the typed public `custy run`
argument accepts it.

<Terminal title="Choose an execution model" language="bash">

{`# One focused operation
custy changelog generate

# One supported coordinated pipeline
custy --dry-run run dev

# Multiple public profiles are merged into one ordered pipeline
custy --dry-run run commit push`}

</Terminal>

</Section>

---

<InfoBox
    variant="warning"
    title="The workflow command group is experimental"
>

`custy workflow` is separate from the stable public Run profiles. Its current
branch workflow is still under development and has a known step-registration
mismatch, so do not treat it as a supported replacement for `custy run dev`,
`custy run release`, or `custy run full`.

</InfoBox>

---

<Section
    title="Choose the Smallest Suitable Operation"
>

- Choose a direct command when you need its command-specific inputs or one
  focused result.
- Choose `custy run PROFILE` when the desired result spans multiple registered
  stages.
- Combine public profiles only when you understand their merged requirements
  and side effects.
- Treat the workflow command group as experimental until its documented
  limitation is resolved.

</Section>

---

<Section
    title="Continue"
>

<RelatedCommands
    commands={[
        {title: "Command Reference", href: "../commands/overview", description: "Choose a focused public Custy command."},
        {title: "Run Overview", href: "../commands/run/overview", description: "Execute supported public pipeline profiles."},
        {title: "Pipeline Guide", href: "../pipelines/overview", description: "Understand resolution, execution, profile flows, and internal steps."},
        {title: "Workflow Command", href: "../commands/workflow/overview", description: "Review the experimental command group's current limitation."},
    ]}
/>

</Section>
