---
title: "Configuration Resolution"
description: "Understand how Custy resolves explicit CLI values, project configuration, and built-in defaults."
sidebar_position: 2
sidebar_key: custy-concepts-configuration-resolution

tags:
  - custy
  - configuration
  - defaults

keywords:
  - custy configuration priority
  - custy config.toml
  - custy option resolution
---

import {
    CommandHero,
    CommandMetadata,
    InfoBox,
    PropertyTable,
    RelatedCommands,
    Section,
    Terminal,
    WorkflowTimeline,
} from "@site/src/components/docs";

<CommandHero
    command="CLI → config.toml → default"
    title="Configuration Resolution"
    description="Understand which value Custy uses when the CLI, project configuration, and application defaults overlap."
    category="Core Concepts"
    status="Essential"
    syntax="custy [GLOBAL OPTIONS] COMMAND [OPTIONS]"
    tags={[
        "configuration priority",
        "project defaults",
        "CLI overrides",
    ]}
    links={[
        {label: "Initialize Config", href: "../commands/init/configuration"},
        {label: "Property Reference", href: "../configuration/reference"},
        {label: "Run Options", href: "../commands/run/options"},
    ]}
/>

<CommandMetadata
    items={[
        {label: "Highest Priority", value: "Explicit CLI value", icon: "terminal"},
        {label: "Project File", value: ".config/custy/config.toml", icon: "configuration"},
        {label: "Root Table", value: "[tool.custy]", icon: "file"},
        {label: "Load Scope", value: "Once per process", icon: "dependency"},
    ]}
/>

## Overview

Custy resolves configurable values in a predictable order. An explicit CLI
value wins, the project configuration supplies a reusable value when the CLI
does not, and a built-in default is used when neither source provides one.

<InfoBox
    variant="tip"
    title="Use configuration for repeatability"
>

Keep stable project choices in `.config/custy/config.toml`. Use CLI options for
intentional one-run overrides that should not change the project's normal
behavior.

</InfoBox>

---

<Section
    title="Resolution Priority"
>

<PropertyTable
    items={[
        {
            property: "1. Explicit CLI value",
            type: "Highest priority",
            description: "A value supplied for the current invocation overrides the corresponding project setting.",
        },
        {
            property: "2. Project configuration",
            type: ".config/custy/config.toml",
            description: "The value under [tool.custy] is used when the matching CLI value is unspecified.",
        },
        {
            property: "3. Built-in default",
            type: "Fallback",
            description: "The command's default is used when neither the CLI nor configuration supplies a value.",
        },
    ]}
/>

</Section>

---

<Section
    title="How Resolution Works"
>

<WorkflowTimeline
    title="Value Selection"
    items={[
        {
            title: "Read the CLI value",
            description: "Use an explicit value immediately when the command supplied one.",
            status: "current",
        },
        {
            title: "Read project configuration",
            description: "When the CLI value is unspecified, look up the corresponding path under [tool.custy].",
            status: "pending",
        },
        {
            title: "Apply the built-in default",
            description: "Use the command's fallback when the configuration path is also absent.",
            status: "pending",
        },
        {
            title: "Validate the result",
            description: "A required value that remains unresolved raises a configuration error.",
            status: "pending",
        },
    ]}
/>

</Section>

---

<Section
    title="Configuration File Shape"
>

Custy extracts its settings from the `[tool.custy]` table. Nested configuration
paths become nested TOML tables.

<Terminal title=".config/custy/config.toml" language="toml">

{`[tool.custy.cli.execution]
dry_run = true

[tool.custy.logging]
level = "INFO"`}

</Terminal>

With this configuration, supported commands inherit dry-run behavior and the
`INFO` log level when the current invocation does not explicitly override
those values.

</Section>

---

<InfoBox
    variant="warning"
    title="Not every option uses all three sources"
>

Resolution is defined by each command's resolver. Some inputs are CLI-only,
some configuration values use a built-in fallback, and required values may
fail instead of receiving a default. Read the affected command's configuration
and options pages before relying on a fallback.

</InfoBox>

---

<Section
    title="Missing and Invalid Configuration"
>

- A missing configuration file is treated as an empty project configuration;
  commands can continue when their remaining values have defaults.
- Invalid TOML or an invalid value is an error and should be corrected rather
  than silently ignored.
- The configuration object is cached for the lifetime of the Custy process.
  A new CLI invocation reads the file again.
- An unspecified option normally falls through to configuration. An explicit
  Boolean choice can remain an intentional CLI override.

</Section>

---

<Section
    title="Continue"
>

<RelatedCommands
    commands={[
        {title: "Init Configuration", href: "../commands/init/configuration", description: "Generate and understand project-specific Custy resources."},
        {title: "All Configuration Properties", href: "../configuration/reference", description: "Find every full TOML path, default, and implementation status."},
        {title: "Run Options", href: "../commands/run/options", description: "Review public pipeline option behavior."},
        {title: "Changelog Configuration", href: "../commands/changelog/generate/configuration", description: "Configure generated changelog content and paths."},
        {title: "Version Configuration", href: "../commands/version/configuration", description: "Configure strategy and version synchronization behavior."},
    ]}
/>

</Section>
