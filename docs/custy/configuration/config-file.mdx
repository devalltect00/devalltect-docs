---
title: "Configuration File"
description: "Understand how Custy creates, loads, owns, and safely replaces project configuration."
sidebar_position: 2
sidebar_key: custy-configuration-file-v2
tags: [custy, configuration, init, toml]
keywords: [custy init --mode config, packaged config, project config]
---

import {
    CommandHero,
    CommandMetadata,
    InfoBox,
    PropertyTable,
    RelatedCommands,
    Section,
    Terminal,
    WorkflowTimeline,
} from "@site/src/components/docs";

<CommandHero
    command="custy init"
    title="Configuration File"
    description="Separate Custy's packaged initialization source from the editable configuration owned by your project."
    category="Configuration"
    status="Essential"
    syntax="custy init [--mode config] [--ask | --force]"
    tags={["ownership", "initialization", "safe replacement"]}
    links={[
        {label: "Initialization", href: "./initialization"},
        {label: "Resolution", href: "./resolution"},
        {label: "All Properties", href: "./reference"},
    ]}
/>

<CommandMetadata
    items={[
        {label: "Packaged Source", value: "app/templates/config.toml", icon: "package"},
        {label: "Project Copy", value: ".config/custy/config.toml", icon: "configuration"},
        {label: "Format", value: "TOML", icon: "file"},
        {label: "Loaded", value: "Once per process", icon: "dependency"},
    ]}
/>

## Overview

Custy ships `app/templates/config.toml` inside the application package. During
initialization it copies that content to `.config/custy/config.toml`. Users
edit the project copy; they do not need access to the installed source file.

---

<Section title="Ownership">

<PropertyTable
    items={[
        {property: "app/templates/config.toml", type: "Application source", description: "Packaged with Custy and used to create a fresh project configuration. Application upgrades may replace it."},
        {property: ".config/custy/config.toml", type: "Project-owned copy", description: "Loaded by commands and intended for team-specific edits, review, and version control."},
        {property: "app/templates/changelog/changelog.j2", type: "Application source", description: "Packaged source for the default changelog layout."},
        {property: ".config/custy/templates/changelog/changelog.j2", type: "Project-owned copy", description: "Editable Jinja template loaded through the configured path."},
    ]}
/>

The current packaged and generated configuration files have the same logical
TOML content. Their byte hashes can differ because of line-ending conversion.

</Section>

---

<Section title="Write Behavior">

<WorkflowTimeline
    items={[
        {title: "Missing resource", description: "Custy creates it from the packaged source.", status: "completed"},
        {title: "Existing resource, normal mode", description: "Custy preserves the project copy and skips replacement.", status: "current"},
        {title: "Existing resource with --ask", description: "Custy asks before replacing an individual file.", status: "pending"},
        {title: "Existing resource with --force", description: "Custy replaces selected resources without prompting.", status: "pending"},
    ]}
/>

<InfoBox variant="danger" title="Force can erase project customization">

Commit, copy, or diff `.config/custy/` before running forced initialization.
The operation is designed to overwrite selected existing resources.

</InfoBox>

</Section>

---

<Section title="TOML Root and Loading">

<Terminal title="Required namespace shape" language="toml">

{`[tool.custy]

[tool.custy.project]
project_source = "auto"

[tool.custy.cli.execution]
dry_run = false`}

</Terminal>

- The central loader exposes values below `[tool.custy]`.
- Missing configuration is treated as an uninitialized project and loads an
  empty mapping. Use `custy init` when project-owned defaults are required.
- Invalid TOML raises a configuration error.
- Relative paths are normally evaluated from the current working directory.
- A new Custy process reloads changes made to the file.
- Keep secrets and Git credentials out of project configuration.

</Section>

---

<Section title="Continue">

<RelatedCommands
    commands={[
        {title: "Initialization", href: "./initialization", description: "Configure default force and prompt behavior."},
        {title: "Resolution", href: "./resolution", description: "Understand CLI, file, and built-in priority."},
        {title: "Init Generated Files", href: "../commands/init/generated-files", description: "Review every resource created by initialization."},
        {title: "Validate", href: "../commands/validate/overview", description: "Check project readiness after editing configuration."},
    ]}
/>

</Section>
