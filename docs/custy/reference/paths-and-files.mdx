---
title: "Paths and Files"
description: "Reference Custy's project configuration, templates, version file, changelog, backups, logs, packaged resources, and working-directory behavior."
sidebar_position: 7
sidebar_key: custy-reference-paths-files
tags: [custy, paths, files]
keywords: [Custy file paths, .config custy, Custy templates]
---

import {
    CommandHero,
    CommandMetadata,
    InfoBox,
    PropertyTable,
    RelatedCommands,
    Section,
} from "@site/src/components/docs";

<CommandHero
    command=".config/custy/"
    title="Paths and Files"
    description="Locate the project-owned files Custy reads, writes, backs up, logs, and initializes."
    category="Reference"
    status="Default Path Index"
    syntax="project root / relative path"
    tags={["configuration", "templates", "backups", "outputs"]}
    links={[
        {label: "Configuration File", href: "../configuration/config-file"},
        {label: "Template Files", href: "../configuration/cli/templates/files"},
        {label: "Generated Files", href: "../commands/init/generated-files"},
    ]}
/>

<CommandMetadata
    items={[
        {label: "Project Config", value: ".config/custy/config.toml", icon: "configuration"},
        {label: "Primary Output", value: "CHANGELOG.md", icon: "release"},
        {label: "Default Version", value: "app/__version__.py", icon: "python"},
        {label: "Default Log", value: "logs/custy.log", icon: "file"},
    ]}
/>

## Project-Owned Paths

<PropertyTable
    title="Active Defaults"
    items={[
        {property: ".config/custy/config.toml", type: "TOML", description: "Project-owned configuration loaded from the current working tree."},
        {property: ".config/custy/templates/changelog/changelog.j2", type: "Jinja template", description: "Template used by ChangelogGenerator to render the complete Markdown document."},
        {property: ".config/custy/templates/commit-message.txt", type: "Text", description: "Editable commit-message source and optional pending changelog content."},
        {property: ".config/custy/templates/tag-message.txt", type: "Text", description: "Editable annotated-tag message source."},
        {property: ".config/custy/templates/backups/commit/", type: "Directory", description: "Default timestamped commit-message backup destination."},
        {property: ".config/custy/templates/backups/tag/", type: "Directory", description: "Default timestamped tag-message backup destination."},
        {property: "app/__version__.py", type: "Python source", description: "Generated and configured default version file. Initialization may select the detected project-source directory or fall back to src/__version__.py."},
        {property: "CHANGELOG.md", type: "Markdown", description: "Fixed active changelog output path in the project root."},
        {property: "logs/custy.log", type: "Rotating log", description: "Built-in log path; both directory and filename can be changed through tool.custy.logging.file."},
    ]}
/>

---

<Section title="Initialization Directories">

Depending on the selected Init mode, Custy can create:

- `.config/custy/`
- `.config/custy/templates/`
- `.config/custy/templates/backups/commit/`
- `.config/custy/templates/backups/tag/`
- `.config/custy/templates/changelog/`
- `.config/custy/templates/examples/commit_message/`
- `.config/custy/templates/examples/tag_message/`

The packaged sources under `app/templates/` belong to the installed Custy
package. Initialization copies or renders those resources into the user's
project; users should customize the project-owned copies instead of editing an
installed package.

</Section>

---

<Section title="Resolution Rules">

- Run Custy from the intended project root. Most default paths are relative to
  the current working directory.
- `tool.custy.project.project_source` is converted to an absolute path and must
  exist as a directory when the shared project-source constant is imported.
- CLI path inputs normally override configuration, which overrides a built-in
  default.
- The version-file path is configurable under
  `tool.custy.cli.paths.version_file`.
- Template and backup paths are configurable under
  `tool.custy.cli.templates.file` and `.directory`.
- The active changelog destination is `CHANGELOG.md`; the template path is
  configurable, but this output filename is not currently exposed as a normal
  project property.

</Section>

---

<InfoBox variant="warning" title="Treat force initialization as replacement">

Project-owned paths may contain customized content. Preview the Init plan and
review the overwrite policy before using `custy init --force`; keep an external
backup for configuration or templates that cannot be recreated.

</InfoBox>

<InfoBox variant="note" title="Current tag-backup path limitation">

The tag-backup workflow resolves a configurable tag backup directory, but one
active engine call still passes the built-in tag-backup constant to the file
writer. Verify the actual destination before depending on a custom tag backup
directory.

</InfoBox>

---

<Section title="Continue">

<RelatedCommands
    commands={[
        {title: "Generated Files", href: "../commands/init/generated-files", description: "Review which Init mode creates each project resource."},
        {title: "Configuration File", href: "../configuration/config-file", description: "Understand project ownership and safe replacement."},
        {title: "Template Files", href: "../configuration/cli/templates/files", description: "Configure changelog, commit-message, and tag-message paths."},
        {title: "Logging Files", href: "../configuration/logging/file", description: "Configure log path, level, rotation size, count, and format."},
    ]}
/>

</Section>
