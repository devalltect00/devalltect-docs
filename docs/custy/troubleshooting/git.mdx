---
title: "Git Troubleshooting"
description: "Resolve repository detection, staging, commit, tag, remote, authentication, push, branch, and cleanup problems safely."
sidebar_position: 2
sidebar_key: custy-troubleshooting-git
tags: [custy, troubleshooting, Git]
keywords: [Custy Git error, Custy push failed, Custy tag conflict]
---

import {
  CommandHero,
  CommandMetadata,
  InfoBox,
  KeyValueTable,
  RelatedCommands,
  Section,
  Terminal,
} from "@site/src/components/docs";

<CommandHero
  command="git status"
  title="Git Troubleshooting"
  description="Verify local and remote repository state before retrying commit, tag, push, cleanup, or release behavior."
  category="Troubleshooting"
  status="Repository Recovery"
  syntax="inspect local state -> inspect destinations -> correct -> verify"
  tags={["staging", "tags", "remotes", "branches"]}
  links={[
    { label: "Troubleshooting Hub", href: "./overview" },
    { label: "Commit", href: "../commands/commit/troubleshooting" },
    { label: "Tag", href: "../commands/tag/troubleshooting" },
    { label: "Push", href: "../commands/push/troubleshooting" },
  ]}
/>

<CommandMetadata
  items={[
    { label: "Repository Service", value: "GitService", icon: "git" },
    {
      label: "Primary Evidence",
      value: "Git state and remote response",
      icon: "terminal",
    },
    { label: "Remote Verification", value: "Per destination", icon: "network" },
    { label: "Force Recovery", value: "Policy approval required", icon: "security" },
  ]}
/>

## Scope of This Page

Use this page when a problem crosses command boundaries or when you need to
inspect shared repository, reference, branch, or remote state. For an error
that clearly belongs to one invocation, use the linked Commit, Tag, Push, or
Branch Cleanup guide for focused recovery steps.

## Inspect Before Retrying

<Terminal title="Local and remote evidence" language="bash">

{`git status --short --branch
git diff
git diff --staged
git log -3 --decorate --oneline
git tag --points-at HEAD
git remote -v
git branch --all`}

</Terminal>

---

<Section title="Repository and Staging">

<KeyValueTable
  items={[
    {
      key: "NOT_GIT_REPO",
      value:
        "Run Custy from the intended repository root and confirm git status works there.",
    },
    {
      key: "NO_STAGED_FILES or NO_STAGED_CHANGES",
      value:
        "Inspect the index. Stage only reviewed files or adjust auto_stage and stage_mode deliberately.",
    },
    {
      key: "INVALID_STAGE_MODE",
      value:
        "Use the supported all, update, manual, or none behavior for the selected command.",
    },
    {
      key: "AUTO_STAGE_FAILED or STAGING_FAILED",
      value:
        "Read the underlying Git response, file permissions, ignore rules, and repository state before repeating.",
    },
    {
      key: "Unexpected staged content",
      value:
        "Stop before commit or push; compare git diff and git diff --staged, then reconcile according to project policy.",
    },
  ]}
/>

</Section>

---

<Section title="Commits and Message Validation">

- Confirm the configured commit-message path exists and contains reviewed
  content.
- Read the first commit-format or Commitizen validation message rather than
  bypassing the entire validation profile.
- Verify whether the commit already exists before retrying a partial pipeline.
- Do not rewrite a pushed commit automatically; follow the repository's shared
  history policy.

See [Commit Troubleshooting](../commands/commit/troubleshooting) for the focused
message, editor, staging, changelog, and content checks.

</Section>

---

<Section title="Tags">

<Terminal title="Inspect a tag locally and remotely" language="bash">

{`git show --decorate <tag>
git ls-remote --tags origin <tag>`}

</Terminal>

If a tag already exists, compare its target and annotation before choosing a
new approved version. Never move or replace a published tag casually; release
assets and CI/CD may already depend on it.

</Section>

---

<Section title="Remotes, Authentication, and Push">

- `REMOTE_NOT_FOUND` or `MAIN_REMOTE_MISSING`: compare `git remote -v` with
  `tool.custy.git` and the Push command's resolved destinations.
- Authentication failure: test Git directly and repair the credential manager,
  SSH agent, provider permission, or CI secret without placing credentials in
  Custy configuration.
- Rejected update: read the remote response for non-fast-forward history,
  required review, branch protection, or missing permission.
- Partial multi-remote success: verify every destination independently and
  recover only failed destinations.
- Do not force-push unless the repository's explicit policy permits it.

</Section>

---

<Section title="Branch Cleanup">

No deletion can be correct behavior when a branch is protected, outside the
configured prefixes, too new, not in the selected merge state, or the command
is in dry-run mode. Review candidates before changing filters, and never weaken
protected-branch safeguards simply to make cleanup remove more branches.

</Section>

---

<InfoBox variant="danger" title="Published state requires coordinated recovery">

Stop and ask a repository owner before deleting remote branches or tags,
force-pushing, moving shared references, or rewriting published commits.

</InfoBox>

---

<Section title="Continue">

<RelatedCommands
  commands={[
    {
      title: "Commit Troubleshooting",
      href: "../commands/commit/troubleshooting",
      description: "Resolve staging, editor, message, changelog, and commit failures.",
    },
    {
      title: "Tag Troubleshooting",
      href: "../commands/tag/troubleshooting",
      description: "Resolve version inputs, validation, and tag conflicts.",
    },
    {
      title: "Push Troubleshooting",
      href: "../commands/push/troubleshooting",
      description:
        "Resolve destination, authentication, rejection, and tag-push behavior.",
    },
    {
      title: "Branch Cleanup Troubleshooting",
      href: "../commands/cleanup/branch/troubleshooting",
      description: "Explain skipped or retained branches and safe filter changes.",
    },
  ]}
/>

</Section>
