---
title: "Everyday Development"
description: "Use Custy's Dev profile safely for reviewed commit-and-branch-push work."
sidebar_position: 2
sidebar_key: custy-user-guide-everyday-development
tags: [custy, development, dev profile]
keywords: [custy run dev, daily development workflow]
---

import {
    CommandChecklist,
    CommandHero,
    CommandMetadata,
    FeatureGrid,
    Icon,
    InfoBox,
    RelatedCommands,
    Section,
    Terminal,
    WorkflowTimeline,
} from "@site/src/components/docs";

<CommandHero
    command="custy run dev"
    title="Everyday Development"
    description="Take reviewed working-tree changes through commit-message validation, commit creation, and branch push."
    category="User Guide"
    status="Preview First"
    syntax="custy --dry-run run dev [OPTIONS]"
    tags={["daily workflow", "commit", "push"]}
    links={[
        {label: "Dev Pipeline", href: "../pipelines/development-pipeline"},
        {label: "Run Dev Reference", href: "../commands/run/dev/overview"},
        {label: "Recovery", href: "./troubleshooting-workflows"},
    ]}
/>

<CommandMetadata
    items={[
        {label: "Creates Commit", value: "Yes", icon: "git"},
        {label: "Pushes", value: "Yes", icon: "network"},
        {label: "Applies Version", value: "No", icon: "note"},
        {label: "Creates Tag", value: "No", icon: "note"},
    ]}
/>

## Goal

Use the Dev profile when reviewed daily work should create one local commit and
finish with a remote branch push. Release artifacts, changelog generation,
version changes, and tags remain outside this profile.

<InfoBox variant="warning" title="Dev is not local-only">

Normal execution ends with `push_step`. Confirm the destination during dry-run
and review the [Push documentation](../commands/push/overview) before using Dev
on a repository whose remotes you have not verified.

</InfoBox>

---

<Section title="Before You Begin">

<CommandChecklist
    items={[
        {title: "Custy resources exist", description: "The project configuration and commit-message file resolve as intended."},
        {title: "The active branch is correct", description: "The branch shown by Git is the branch you intend to commit and push."},
        {title: "Changes are understood", description: "Review unstaged and staged differences so unrelated work is not included."},
        {title: "The commit message is ready", description: "The configured commit-message file contains a reviewed message."},
        {title: "Remotes are intentional", description: "Every configured or selected destination points to the expected repository."},
    ]}
/>

<Terminal title="Development preflight" language="bash">

{`git status --short --branch
git diff
git diff --staged
git remote -v
custy validate`}

</Terminal>

</Section>

---

<Section title="Recommended Journey">

<WorkflowTimeline
    title="Daily Development"
    items={[
        {title: "Review the working tree", description: "Confirm branch, changes, staged state, commit message, and remotes.", status: "completed"},
        {title: "Preview Dev", description: "Run the complete profile in global dry-run mode and read the resolved files and destinations.", status: "completed"},
        {title: "Review the intent", description: "Check commit-message editing, backup, staging, commit, and branch-push messages.", status: "completed"},
        {title: "Execute normally", description: "Run Dev without dry-run only after every reported action is acceptable.", status: "current"},
        {title: "Verify local and remote state", description: "Inspect the final status and commit, then confirm the destination branch received it.", status: "pending"},
    ]}
/>

<Terminal title="Preview, then execute" language="bash">

{`custy --dry-run run dev
custy run dev`}

</Terminal>

</Section>

---

<Section title="What Dev Actually Includes">

<FeatureGrid
    title="Profile Effects"
    items={[
        {title: "Validation", icon: <Icon name="configuration" />, children: "Validate repository, commit-message, hook, and remote readiness."},
        {title: "Commit Message", icon: <Icon name="file" />, children: "Open and validate only the configured commit-message file."},
        {title: "Backup and Commit", icon: <Icon name="package" />, children: "Back up the commit message, prune retained backups, stage, and commit."},
        {title: "Git Publication", icon: <Icon name="network" />, children: "Push branch HEAD to resolved destinations without pushing a tag."},
    ]}
/>

Dev does not prepare or apply versions, initialize a release workflow, generate
release artifacts or a changelog, edit a tag message, create a tag, or push a
tag. The [exact 13-step order](../pipelines/development-pipeline) explains every
internal step.

</Section>

---

<Section title="Verify Completion">

<Terminal title="Local verification" language="bash">

{`git status --short --branch
git log -1 --decorate --stat
git tag --points-at HEAD
git remote -v`}

</Terminal>

Expect the intended commit to exist and the working tree to reflect only any
deliberately uncommitted work. Dev should not create a new release tag. Confirm
the remote result using your repository host or an appropriate Git fetch and
comparison workflow.

</Section>

---

<InfoBox variant="danger" title="Do not blindly rerun a failed Dev profile">

An edited message, backups, the Git index, a commit, or a remote push may already
exist. Use [Troubleshooting Workflows](./troubleshooting-workflows) to identify
the last completed boundary first.

</InfoBox>

---

<Section title="Continue">

<RelatedCommands
    commands={[
        {title: "Development Pipeline", href: "../pipelines/development-pipeline", description: "Inspect the exact 13-step sequence and its omissions."},
        {title: "Run Dev Examples", href: "../commands/run/dev/examples", description: "Use practical Dev invocations."},
        {title: "Commit Command", href: "../commands/commit/overview", description: "Choose a focused local commit operation."},
        {title: "Preparing Releases", href: "./preparing-releases", description: "Move from daily work to a versioned tagged publication."},
    ]}
/>

</Section>
