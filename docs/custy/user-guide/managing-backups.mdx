---
title: "Managing Backups"
description: "Create, verify, retain, clean, and restore Custy's commit and tag message backups."
sidebar_position: 4
sidebar_key: custy-user-guide-managing-backups
tags: [custy, backups, retention]
keywords: [custy backup all, custy cleanup backups, restore Custy backup]
---

import {
    CommandChecklist,
    CommandHero,
    CommandMetadata,
    FeatureGrid,
    Icon,
    InfoBox,
    RelatedCommands,
    Section,
    Terminal,
    WorkflowTimeline,
} from "@site/src/components/docs";

<CommandHero
    command="custy backup"
    title="Managing Backups"
    description="Protect Custy's commit and tag message files and keep their timestamped history within an intentional retention policy."
    category="User Guide"
    status="Maintenance Journey"
    syntax="custy backup <commit|tag|all>"
    tags={["message files", "timestamped copies", "retention", "restore"]}
    links={[
        {label: "Backup Reference", href: "../commands/backup/overview"},
        {label: "Backup Cleanup", href: "../commands/cleanup/backup/overview"},
        {label: "Backup Directories", href: "../configuration/cli/templates/directories"},
    ]}
/>

<CommandMetadata
    items={[
        {label: "Protected Inputs", value: "Commit and tag messages", icon: "file"},
        {label: "Backup Format", value: "Timestamped .bak.txt", icon: "copy"},
        {label: "Default Retention", value: "10 per selected category", icon: "timeline"},
        {label: "Git History", value: "Not created by backup command", icon: "git"},
    ]}
/>

## Goal

Custy backups preserve configured commit-message and tag-message files before
you edit conventions or prepare releases. They are focused recovery copies, not
full project, repository, database, or remote backups.

---

<Section title="Choose the Backup Scope">

<FeatureGrid
    title="Backup Commands"
    items={[
        {title: "Commit Message", icon: <Icon name="git" />, children: <><code>custy backup commit</code> preserves the configured commit-message file.</>},
        {title: "Tag Message", icon: <Icon name="release" />, children: <><code>custy backup tag</code> preserves the configured tag-message file.</>},
        {title: "Both Messages", icon: <Icon name="package" />, children: <><code>custy backup all</code> runs both focused backup profiles.</>},
        {title: "Retention Cleanup", icon: <Icon name="timeline" />, children: <><code>custy cleanup backups</code> removes older copies while keeping the configured number.</>},
    ]}
/>

</Section>

---

<Section title="Recommended Backup Journey">

<WorkflowTimeline
    title="Protect and Retain"
    items={[
        {title: "Review source and destination paths", description: "Confirm message files and their separate backup directories in project configuration.", status: "completed"},
        {title: "Preview creation", description: "Dry-run the selected backup command and confirm the timestamped target.", status: "completed"},
        {title: "Create the backup", description: "Run the focused or all-message backup command normally.", status: "current"},
        {title: "Verify the copy", description: "Confirm the expected file exists and contains the intended source content.", status: "pending"},
        {title: "Preview retention cleanup", description: "Dry-run cleanup before removing older backups.", status: "pending"},
    ]}
/>

<Terminal title="Backup both message files" language="bash">

{`custy --dry-run backup all
custy backup all`}

</Terminal>

Backups use timestamped names derived from the source stem. If the configured
source file is missing, Custy skips that backup rather than inventing content.

</Section>

---

<Section title="Retention and Cleanup">

The generated configuration keeps the 10 newest files in each selected backup
category. `--keep` temporarily overrides that count; `--type` selects `commit`,
`tag`, or `all`.

<Terminal title="Preview and apply retention" language="bash">

{`custy --dry-run cleanup backups --type all --keep 10
custy cleanup backups --type all --keep 10`}

</Terminal>

Review the configured `backups_commit` and `backups_tag` directories before
cleanup. Location and retention are separate settings.

</Section>

---

<InfoBox variant="warning" title="Pipelines can clean backups automatically">

Commit, Tag, Dev, Release, and Full include backup and retention-cleanup steps.
An automatic backup may therefore be followed immediately by pruning of older
copies. Review retention before relying on long backup history.

</InfoBox>

---

<Section title="Restore a Message File">

Custy currently creates and cleans these backups; restoration is a deliberate
file operation performed by the user. To restore safely:

1. Stop any active Custy workflow.
2. Identify the correct timestamped backup.
3. Compare it with the active message file.
4. Preserve the current active file if it may still be needed.
5. Copy the reviewed backup content into the configured active message file.
6. Run the relevant validation or dry-run before continuing.

Do not restore a `.bak.txt` file based only on its timestamp—inspect its content
and verify whether it belongs to commit or tag messaging first.

</Section>

---

<Section title="Backup Checklist">

<CommandChecklist
    items={[
        {title: "Source exists", description: "The configured commit or tag message file exists and contains the version you intend to preserve."},
        {title: "Destination is correct", description: "Commit and tag backups resolve to the intended separate directories."},
        {title: "Retention is sufficient", description: "The keep count preserves enough history for your workflow."},
        {title: "The copy is verified", description: "Inspect the new backup before changing the source file."},
        {title: "Cleanup is previewed", description: "Use dry-run before removing older recovery copies."},
    ]}
/>

</Section>

---

<Section title="Continue">

<RelatedCommands
    commands={[
        {title: "Backup Files", href: "../commands/backup/backup-files", description: "Review exact paths, naming, and file behavior."},
        {title: "Backup Cleanup Workflow", href: "../commands/cleanup/backup/workflow", description: "Understand retention selection and deletion."},
        {title: "Template Backup Directories", href: "../configuration/cli/templates/directories", description: "Configure commit and tag backup locations."},
        {title: "Backup Cleanup Configuration", href: "../configuration/cli/cleanup/backup", description: "Configure category and keep count."},
    ]}
/>

</Section>
