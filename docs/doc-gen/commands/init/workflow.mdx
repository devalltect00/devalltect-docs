---
title: "Workflow"
description: "Workflow: doc-gen init"
sidebar_position: 5
sidebar_key: doc-gen-init
tags: [doc-gen, command]
---

import { CommandHero, InfoBox, Section, Terminal } from "@site/src/components/docs";

<CommandHero
  command="doc-gen init"
  title="Workflow"
  description="Create the project-owned Doc Gen configuration file."
  category="Initialization"
  status="Recommended"
  syntax="doc-gen init [OPTIONS]"
  links={[
    { label: "Overview", href: "./overview" },
    { label: "Syntax", href: "./syntax" },
    { label: "Options", href: "./options" },
    { label: "Examples", href: "./examples" },
  ]}
/>

### 1. Stage 1

Resolve defaults.

### 2. Stage 2

Build the selected configuration-file plan.

### 3. Stage 3

Dry-run displays and stops.

### 4. Stage 4

Live mode creates or updates the configuration according to `--ask` and `--force`.

<InfoBox variant="warning" title="Safety boundary">
  Dry-run never creates directories, writes files, replaces content, or prompts.
</InfoBox>
