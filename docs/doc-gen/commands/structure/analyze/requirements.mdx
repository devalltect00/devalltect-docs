---
title: "Requirements"
description: "A readable target directory. Enough access to inspect repository entries. Appropriate ignore rules for large/generated directories."
sidebar_position: 2
sidebar_key: doc-gen-structure-analyze
tags: [doc-gen, command]
---

import {CommandHero, InfoBox, Section, Terminal} from "@site/src/components/docs";

<CommandHero command="doc-gen structure analyze" title="Requirements" description="Before you run it" category="Project Structure" status="Read Only" syntax="doc-gen structure analyze [OPTIONS]" links={[{label:"Overview",href:"./overview"},{label:"Options",href:"./options"},{label:"Workflow",href:"./workflow"},{label:"Examples",href:"./examples"}]} />

- A readable target directory.
- Enough access to inspect repository entries.
- Appropriate ignore rules for large/generated directories.

<InfoBox variant="tip" title="Dry run">Analyze is inherently read-only. It may scan and calculate metadata, but it does not invoke the Markdown writer.</InfoBox>

