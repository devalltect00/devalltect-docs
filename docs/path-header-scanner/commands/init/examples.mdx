---
title: "Examples"
description: "Examples: path-header-scanner init"
sidebar_position: 7
sidebar_key: phs-init
tags: [path-header-scanner, command]
---

import { CommandHero, InfoBox, Section, Terminal } from "@site/src/components/docs";

<CommandHero
  command="path-header-scanner init"
  title="Examples"
  description="Dry-run never creates directories, writes files, replaces content, or prompts."
  category="Initialization"
  status="Recommended"
  syntax="path-header-scanner init [OPTIONS]"
  links={[
    { label: "Overview", href: "./overview" },
    { label: "Options", href: "./options" },
    { label: "Workflow", href: "./workflow" },
    { label: "Examples", href: "./examples" },
  ]}
/>

### 1. Preview

<Terminal language="bash">{`path-header-scanner init --dry-run`}</Terminal>

**Result:** Shows planned resources.

### 2. Initialize

<Terminal language="bash">{`path-header-scanner init`}</Terminal>

**Result:** Creates resolved resources.

### 3. Configuration only

<Terminal language="bash">{`path-header-scanner init --mode config`}</Terminal>

**Result:** Creates configuration.

### 4. Force

<Terminal language="bash">{`path-header-scanner init --mode all --force`}</Terminal>

**Result:** Replaces selected files.
