---
title: "Requirements"
description: "Docker engine running. Valid Dockerfile/build context. Registry authentication and image permission. At least one Git tag."
sidebar_position: 2
sidebar_key: reflow-dockerize
tags: [reflow, requirements]
---

import {CommandHero, InfoBox, Section, Terminal} from "@site/src/components/docs";

<CommandHero command="reflow dockerize" title="Requirements" description="Before you run it" category="Container Publishing" status="External Mutation" syntax="reflow dockerize [OPTIONS]" links={[{label:"Overview",href:"./overview"},{label:"Options",href:"./options"},{label:"Workflow",href:"./workflow"},{label:"Examples",href:"./examples"}]} />

## Before you run it

- Docker engine running.
- Valid Dockerfile/build context.
- Registry authentication and image permission.
- At least one Git tag.

<InfoBox variant="tip" title="Dry run">Dry-run performs discovery and validation without persistent mutation.</InfoBox>

