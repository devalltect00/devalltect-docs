---
title: "Workflow"
description: "Workflow: reflow init"
sidebar_position: 5
sidebar_key: reflow-init
tags: [reflow, workflow]
---

import { CommandHero, InfoBox, Section, Terminal } from "@site/src/components/docs";

<CommandHero
  command="reflow init"
  title="Workflow"
  description="Plan and write project-owned Reflow configuration without changing Git history or remote state."
  category="Initialization"
  status="Recommended"
  syntax="reflow init [OPTIONS]"
  links={[
    { label: "Overview", href: "./overview" },
    { label: "Requirements", href: "./requirements" },
    { label: "Options", href: "./options" },
    { label: "Examples", href: "./examples" },
  ]}
/>

### 1. Stage 1

Resolve CLI, configuration, and internal defaults.

### 2. Stage 2

Build the resource plan for the selected mode.

### 3. Stage 3

Show the plan; dry-run stops before mutation.

### 4. Stage 4

Create or replace resources according to ask and force behavior.

<InfoBox variant="warning" title="Safety boundary">
  Writes only selected initialization resources. Dry-run never creates, replaces, or
  prompts for files.
</InfoBox>
