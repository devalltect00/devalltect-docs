---
title: "Examples"
description: "Examples: reflow releases recover"
sidebar_position: 7
sidebar_key: reflow-releases-recover
tags: [reflow, examples]
---

import {CommandHero, InfoBox, Section, Terminal} from "@site/src/components/docs";

<CommandHero command="reflow releases recover" title="Examples" description="Dry-run performs discovery and validation without persistent mutation." category="Release Recovery" status="GitHub Workflow Recovery" syntax="reflow releases recover [OPTIONS]" links={[{label:"Overview",href:"./overview"},{label:"Options",href:"./options"},{label:"Workflow",href:"./workflow"},{label:"Examples",href:"./examples"}]} />

### 1. Preview

<Terminal language="bash">{`reflow --dry-run --repository-url https://github.com/acme/project.git releases recover`}</Terminal>

**Result:** Shows selections.

### 2. Recover

<Terminal language="bash">{`reflow --repository-url https://github.com/acme/project.git releases recover --yes`}</Terminal>

**Result:** Re-pushes tags.

### 3. Stable limit

<Terminal language="bash">{`reflow --repository-url https://github.com/acme/project.git releases recover --only-stable --limit 5 --yes`}</Terminal>

**Result:** Processes five stable tags maximum.

