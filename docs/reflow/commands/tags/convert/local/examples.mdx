---
title: "Examples"
description: "Examples: reflow tags convert local"
sidebar_position: 7
sidebar_key: reflow-tags-convert-local
tags: [reflow, examples]
---

import { CommandHero, InfoBox, Section, Terminal } from "@site/src/components/docs";

<CommandHero
  command="reflow tags convert local"
  title="Examples"
  description="Dry-run performs discovery and validation without persistent mutation."
  category="Tag Conversion"
  status="Stable"
  syntax="reflow tags convert local [OPTIONS]"
  links={[
    { label: "Overview", href: "./overview" },
    { label: "Options", href: "./options" },
    { label: "Workflow", href: "./workflow" },
    { label: "Examples", href: "./examples" },
  ]}
/>

### 1. Preview

<Terminal language="bash">{`reflow --dry-run -C ../project tags convert local`}</Terminal>

**Result:** Shows replacements only.

### 2. SemVer

<Terminal language="bash">{`reflow -C ../project tags convert local --yes`}</Terminal>

**Result:** Replaces local tag names.

### 3. PEP 440

<Terminal language="bash">{`reflow -C ../project tags convert local --to pep440 --yes`}</Terminal>

**Result:** Uses PEP 440 names.
