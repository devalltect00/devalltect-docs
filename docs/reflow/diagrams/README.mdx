---
title: "Readme"
description: "Diagrams"
sidebar_key: reflow-diagrams
---
# Diagrams

## Overview

This section contains architecture, workflow, and lifecycle diagrams for Reflow.

The diagrams provide a visual representation of how commands, services, and workflows interact throughout the application.

---

# Diagram Categories

The diagrams are organized into the following categories:

```text
Project Overview
Workflow Diagrams
Lifecycle Diagrams
Architecture Diagrams
```

---

# Project Overview

## Reflow Overview

File:

```text
reflow-overview.mmd
```

Purpose:

```text
High-level view of the Reflow ecosystem.
```

Shows:

```text
Install Reflow
Initialize Project
Configure Project
Convert Tags
Replay Releases
Dockerize Images
```

Recommended for:

```text
New users
Project overview
README references
```

---

# Workflow Diagrams

Workflow diagrams explain what happens when a command is executed.

---

## Init Workflow

File:

```text
init-workflow.mmd
```

Documentation:

```text
docs/init/workflow.md
```

Shows:

```text
CLI
Resolver
InitMain
Builder
Generator
Presenter
```

Purpose:

```text
Project initialization workflow.
```

---

## Convert Workflow

File:

```text
convert-workflow.mmd
```

Documentation:

```text
docs/tags/convert/workflow.md
```

Shows:

```text
PEP440 Tag
      ↓
SemVer Tag
```

Purpose:

```text
Version conversion workflow.
```

---

## Replay Workflow

File:

```text
replay-workflow.mmd
```

Documentation:

```text
docs/tags/replay/workflow.md
```

Shows:

```text
Git Tag
      ↓
GitHub Release
```

Purpose:

```text
Release recovery workflow.
```

---

## Dockerize Workflow

File:

```text
dockerize-workflow.mmd
```

Documentation:

```text
docs/dockerize/workflow.md
```

Shows:

```text
Git Tag
      ↓
Docker Image
      ↓
Registry
```

Purpose:

```text
Container publishing workflow.
```

---

# Lifecycle Diagrams

Lifecycle diagrams explain how commands fit together.

---

## Release Lifecycle

File:

```text
release-lifecycle.mmd
```

Documentation:

```text
docs/user-guide/lifecycle.md
```

Shows:

```text
Install
Init
Configure
Convert
Replay
Dockerize
```

Purpose:

```text
End-to-end Reflow workflow.
```

---

# Architecture Diagrams

Architecture diagrams focus on implementation structure.

---

## Architecture Layers

File:

```text
architecture-layers.mmd
```

Documentation:

```text
docs/architecture/workflow.md
```

Shows:

```text
CLI
Resolver
Core
Service
Executor
External Tools
```

Purpose:

```text
Application architecture.
```

---

# Recommended Reading Order

For new users:

```text
Reflow Overview
        ↓
Release Lifecycle
        ↓
Init Workflow
        ↓
Convert Workflow
        ↓
Replay Workflow
        ↓
Dockerize Workflow
```

---

# For Contributors

Recommended order:

```text
Architecture Layers
        ↓
Init Workflow
        ↓
Convert Workflow
        ↓
Replay Workflow
        ↓
Dockerize Workflow
```

This provides a complete understanding of the Reflow architecture and command lifecycle.

---

# Related Documentation

User Guide:

```text
docs/user-guide/
```

Architecture:

```text
docs/architecture/
```

Initialization:

```text
docs/init/
```

Configuration:

```text
docs/configuration.md
```

Roadmap:

```text
docs/project/roadmap.md
```
