---
title: Examples
description: Safe local and remote Reflow command examples.
sidebar_position: 5
sidebar_key: reflow-examples
---

import {CommandHero, InfoBox, RelatedCommands, Section, Terminal} from "@site/src/components/docs";

<CommandHero
    command="reflow"
    title="Workflow Examples"
    description="Preview repository-aware tag, release-recovery, and container workflows before authorizing persistent changes."
    category="Examples"
    status="Dry-run First"
    syntax="reflow [GLOBAL OPTIONS] COMMAND"
    links={[
        {label: "Commands", href: "./commands/overview"},
        {label: "Repository Targets", href: "./concepts/repository-targeting"},
        {label: "Dry-run Safety", href: "./concepts/safety-and-dry-run"},
        {label: "Configuration", href: "./configuration/reference"},
    ]}
/>

<Section title="Replace tag formats">

<Terminal title="Local PEP 440 to SemVer" language="bash">{`# Preview local tag-ref replacement
reflow --dry-run -C ../project tags convert local --to semver

# Apply after reviewing the plan
reflow -C ../project tags convert local --to semver --yes`}</Terminal>

<Terminal title="Remote SemVer to PEP 440" language="bash">{`# A URL is cloned into a managed temporary workspace for discovery
reflow --dry-run --repository-url https://github.com/acme/project.git tags convert remote --to pep440

# Remote scope performs the guarded push; --push is not used
reflow --repository-url https://github.com/acme/project.git tags convert remote --to pep440 --yes`}</Terminal>

</Section>

<Section title="Preview external workflows">

<Terminal language="bash">{`# Find tags whose GitHub releases are missing; do not re-push
reflow --dry-run -C ../project releases recover

# Resolve tagged images; do not build, push, or clean images
reflow --dry-run -C ../project dockerize`}</Terminal>

</Section>

<InfoBox variant="warning" title="Dry-run is not authorization">

Remove `--dry-run` only after verifying the displayed repository, conversion
plan, remote, registry image, and confirmation behavior.

</InfoBox>

<RelatedCommands
    commands={[
        {title: "Local Tag Conversion", href: "./commands/tags/convert/local/examples", description: "More persistent-checkout examples."},
        {title: "Remote Tag Conversion", href: "./commands/tags/convert/remote/examples", description: "More guarded remote replacement examples."},
        {title: "Release Recovery", href: "./commands/releases/recover/examples", description: "Preview and authorize tag re-push recovery."},
        {title: "Dockerize", href: "./commands/dockerize/examples", description: "Preview tagged image publication."},
    ]}
/>
